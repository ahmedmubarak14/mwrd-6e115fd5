import React from 'react';

const MinimalApp: React.FC = () => {
  console.log('MinimalApp: Starting');
  return (
    <div>
      <h1>Minimal App Test</h1>
      <p>This is a test to see if React works at all</p>
    </div>
  );
};

export default MinimalApp;